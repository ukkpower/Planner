services:
  - type: web
    name: cosy-level-planner
    runtime: static
    buildCommand: npm ci && npm run build:render
    staticPublishPath: ./dist
    autoDeployTrigger: commit
    envVars:
      - key: CONVEX_DEPLOY_KEY
        sync: false
